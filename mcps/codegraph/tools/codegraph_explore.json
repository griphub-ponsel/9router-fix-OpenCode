{
  "name": "codegraph_explore",
  "description": "Source of SEVERAL related symbols grouped by file, in one capped call. Query is a bag of symbol/file names (not a question). Returned source is verbatim Read-equivalent — do not re-open shown files. Prefer over chained codegraph_node. Budget: make at most 2 calls for this project (664 files indexed).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Symbol names, file names, or short code terms to explore (e.g., \"AuthService loginUser session-manager\", \"GraphTraverser BFS impact traversal.ts\"). Use codegraph_search first to find relevant names."
      },
      "maxFiles": {
        "type": "number",
        "description": "Maximum number of files to include source code from (default: 12)",
        "default": 12
      },
      "projectPath": {
        "type": "string",
        "description": "Path to a different project with .codegraph/ initialized. If omitted, uses current project. Use this to query other codebases."
      }
    },
    "required": [
      "query"
    ]
  }
}